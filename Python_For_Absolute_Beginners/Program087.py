# Deleting a file in python !!

# We cannot directly delete a file 
#  to delete a file we need to import a library named os and by .remove() we can delete a file 

import os

os.remove("demo8.txt")